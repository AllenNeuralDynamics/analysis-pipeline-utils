
def copy_results_to_s3(metadata, s3_bucket, results_path="/results"):
    """
    Copy results to s3, calculating the prefix from the metadata record.
    Returns metadata record updated with s3 storage location
    """
    # details - does metadata include Processing and DataDescription
    return metadata

